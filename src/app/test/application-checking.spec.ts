import { ApplicationChecking } from '../model/application-checking';

describe('ApplicationChecking', () => {
  it('should create an instance', () => {
    expect(new ApplicationChecking()).toBeTruthy();
  });
});
